{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch LVGL",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build",
        }
    ]
}
